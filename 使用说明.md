# BOSS直聘自动打招呼 —— 使用说明

## 一、环境已就绪（已完成，不用重做）

独立环境在 `E:\AutoPlayGame\py312-airtest`，基于本机已有的 Python 3.12.14：

| 包 | 版本 | 说明 |
|---|---|---|
| airtest | 1.4.3 | 设备连接与操作 |
| onnxocr-ppocrv5 | 0.0.22 | OCR 引擎（PP-OCRv5 的 ONNX 版） |
| onnxruntime | 1.29.0 | 推理后端，**是 extra 依赖，不会随主包自动装** |
| opencv-contrib-python | 4.6.0 | 图像处理 |
| numpy | 1.26.4 | — |

实测性能：整页 OCR **0.51s**，按钮区域裁剪 OCR **0.08s**，模型内置在包里无需下载。

---

## 二、必须先做：让 AirtestIDE 用上这个环境

AirtestIDE 内置的是 Python 3.6，跑不了 OCR。需要把解释器指向新环境：

1. 打开 AirtestIDE → 顶部菜单 **选项 → 设置**
2. 找到 **自定义 Python.exe 路径**（对应配置项 `pythonexe`）
3. 填入：
   ```
   E:\AutoPlayGame\py312-airtest\Scripts\python.exe
   ```
4. 确定并**重启 AirtestIDE**

> 代价：切换后 IDE 的「Airtest 辅助窗」图像录制功能可能失效（它依赖内置的 3.6）。
> 本项目已弃用全部图片素材，不受影响；需要截图时用 IDE 自带截图按钮即可。

配置实际写在 `%LOCALAPPDATA%\AirtestIDE\AirtestIDE\user.json` 的 `pythonexe` 字段。

---

## 三、首次运行：先校准，再执行

### 第 1 步 —— 校准区域（只需做一次）

`config.py` 里的 `ROI_JOB_TITLE`（岗位标题）和 `ROI_COMPANY`（公司名）目前是**推测值**，必须实测确认。

**推荐用 `calibrate_adb.py`（零侵入版）**：只用 `adb screencap` 取帧，不启动 minicap/minitouch，
AirtestIDE 正在投屏时也能安全运行，不会打断连接。

```
E:\AutoPlayGame\py312-airtest\Scripts\python.exe calibrate_adb.py
```

它会自动探测在线设备（多设备时在命令后加 serial 指定）。

> 备选是 IDE 内的 `calibrate.py`，走 airtest 连接，可能占用 IDE 的投屏通道，建议关掉 IDE 再用。

1. 手机打开 BOSS直聘，停在**岗位详情页**
2. 运行 `calibrate_adb.py`
3. 看输出，格式如下：

```
  y= 10.2%~ 13.5%  x= 3.1%~48.0%  0.97  高级软件工程师
  y= 24.1%~ 27.0%  x= 3.1%~60.2%  0.99  某某科技有限公司
  y= 93.8%~ 97.2%  x=28.0%~70.0%  0.98  立即沟通
```

4. 把岗位标题、公司名那两行的 `y=` 范围填回 `config.py`：

```python
ROI_JOB_TITLE = (0.03, 0.10, 0.97, 0.14)   # y 取 10.2%~13.5% 外扩一点
ROI_COMPANY   = (0.03, 0.23, 0.97, 0.28)   # y 取 24.1%~27.0% 外扩一点
```

脚本末尾会自动用当前配置试识别三个区域，输出「空 —— 需要调整!」就说明还没对准。

### 第 2 步 —— 正式运行

手机停在岗位详情页，运行 `auto_greeting.py`：

```
python auto_greeting.py              # 默认 30 轮
python auto_greeting.py --rounds 50  # 指定 50 轮
```

在 AirtestIDE 里直接点运行也可以，轮次取 `config.py` 的 `DEFAULT_ROUNDS`。

---

## 四、主流程

每轮循环做这些事：

1. **按钮区 OCR** —— 底部区域找「立即沟通」或「继续沟通」
   - 出现「继续沟通」→ 已沟通过，**跳过**
   - 两个都没有 → 页面不对，记为失败并截图
2. **黑名单过滤** —— 在标题区 + 公司名区找黑名单词（子串匹配），命中则**跳过**
3. **点击招呼** —— 按归一化坐标点「立即沟通」
4. **校验跳转** —— 轮询等底部「立即沟通」消失（最多 5s），确认已进聊天页
5. **停留返回** —— 停 1.5~2.3s 等招呼语发出，点左上角返回，轮询确认回到详情页
6. **切换下一个** —— 左滑，随机延时 1.5~4s，每 20 个长休息 60s

终止条件：跑满轮次，或**连续失败 3 次**自动停机。

---

## 五、常用调整（都在 config.py）

| 想改什么 | 改哪个参数 |
|---|---|
| 轮次 | `DEFAULT_ROUNDS` / 命令行 `--rounds` |
| 跳过哪些岗位 | `BLACK_KEYWORDS`（子串匹配） |
| 过滤范围 | `FILTER_SCOPE`：`title_company`（推荐）/ `full_page` |
| 点击坐标 | `BTN_COMMUNICATE_POS`、BTN_BACK_POS |
| 滑动距离 | `SWIPE_DX`（当前 **-0.60**，实测不足 60% 屏宽翻不动页） |
| OCR 严格程度 | `OCR_CONF_MIN`（当前 0.6） |
| 反风控节奏 | `DELAY_MIN/MAX`、`LONG_REST_EVERY`、`LONG_REST_SEC` |
| 截图留证 | `SAVE_SKIP_SHOT`、`SAVE_FAIL_SHOT` |

---

## 六、已知坑位

| 现象 | 原因与处理 |
|---|---|
| 滑动后还在同一个岗位 | 位移太小。实测 -0.198 / -0.45 均不触发，`SWIPE_DX` 需 ≥ **-0.60**，且起点要偏右（`SWIPE_START_POS` x=0.80） |
| 一直提示「按钮区未识别到」 | 页面不是岗位详情页，或 `ROI_BUTTON` 范围没覆盖按钮。跑 `calibrate.py` 核对 |
| 点完提示「未离开详情页」 | 按钮位置变了。跑 `calibrate.py` 看「立即沟通」实际 y 范围，更新 `BTN_COMMUNICATE_POS` |
| 中文乱码 / 截图保存失败 | 已用 `imencode + tofile` 规避 Windows 中文路径问题；若仍失败检查目录权限 |
| IDE 报 `No module named 'onnxocr'` | 没配好自定义 Python.exe，回到第二节检查 |
| OCR 识别慢 | 确认 `OCR_USE_CLS = False`（开着会多加载方向分类模型） |

---

## 七、文件说明

| 文件 | 作用 |
|---|---|
| `auto_greeting.py` | 主脚本 |
| `config.py` | 所有可调参数 |
| `ocr_helper.py` | OCR 适配层（归一化 ROI 裁剪、置信度过滤、子串匹配） |
| `calibrate_adb.py` | 区域校准工具（**零侵入，推荐**）—— adb 取帧，不打断 IDE 投屏 |
| `calibrate.py` | 区域校准工具（IDE 内运行版，走 airtest 连接） |
| `auto_greeting_原始备份.py` | 你原来的脚本备份 |
| `img/` | 旧图片素材，已不再使用 |

运行产物：`log/shots/` 存跳过与异常的截图；`log/calibrate_latest.png` 是最近一次校准的原图。

---

## 八、环境自检记录（本次搭建已实测通过）

| 验证项 | 结果 |
|---|---|
| 真机抓屏（adb screencap，1080×2408） | 成功，不影响 IDE 投屏 |
| 真机截图 OCR（34 条文本） | 0.93s |
| 按钮素材 OCR「立即沟通」 | 置信度 1.00 |
| 坐标 (0.49, 0.96) → (529, 2312) vs OCR 实测 | 偏差 3px / 2px |
| 黑名单子串匹配 | 「高级硬件工程师」→ 硬件；「高级软件工程师」→ 不命中 |
